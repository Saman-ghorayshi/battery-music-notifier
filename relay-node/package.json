{
  "name": "battery-relay-node",
  "version": "2.0.0",
  "description": "Self-hostable Node.js + PostgreSQL relay for Battery Music Notifier -- drop-in alternative to the Cloudflare Worker.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "redis": "^6.2.1"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  },
  "license": "MIT"
}
